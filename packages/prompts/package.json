{
  "name": "@synoro/prompts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsup",
    "build:watch": "tsup --watch",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "gen": "bun src/scripts/gen-registry.ts",
    "prebuild": "bun run gen",
    "upload-prompts": "bun run with-env -- bun src/scripts/upload-prompts.ts",
    "dev": "tsc",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "lint": "eslint",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false",
    "with-env": "dotenv -e ../../.env --",
    "update-prompt": "bun run with-env -- bun src/scripts/update-prompt.ts",
    "test:performance": "bun src/scripts/test-performance.ts",
    "benchmark": "bun run test:performance"
  },
  "devDependencies": {
    "@synoro/eslint-config": "workspace:*",
    "@synoro/prettier-config": "workspace:*",
    "@synoro/tsconfig": "workspace:*",
    "dotenv-cli": "10.0.0",
    "eslint": "9.36.0",
    "prettier": "3.6.2",
    "tsup": "8.5.0",
    "typescript": "5.9.2"
  },
  "prettier": "@synoro/prettier-config",
  "dependencies": {
    "@langfuse/client": "^4.2.0",
    "langfuse": "^3.38.5"
  }
}
